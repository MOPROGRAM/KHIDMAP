
"use client";

// This page is no longer used as the app has pivoted from ads to direct provider profiles.
// It is kept to avoid breaking routing until a full cleanup is performed.

export default function DeprecatedEditAdPage() {
    return null;
}
